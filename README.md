# 🚀 **Telegram Web Clone с Админ-панелью для Railway.com**

## 🎯 **ЦЕЛЬ ПРОЕКТА**
Разработан и развернут на **Railway.com** полнофункциональная веб-копия Telegram (web.telegram.org) с полной админ-панелью для управления. Приложение визуально идентично оригиналу (pixel-perfect) и содержит все основные функции мессенджера.

**Ключевые особенности:**
- Pixel-perfect копия Telegram Web с темной/светлой темами
- Полноценная админ-панель для номера `+79918816358`
- Регистрация через SMS (SMSC.RU API)
- Хранение файлов на Railway Volumes
- Реальное время общения через Socket.IO
- Автоматический деплой на Railway

## 👑 **АДМИНИСТРАТИВНАЯ ПАНЕЛЬ ДЛЯ +7991816358**

### **Права администратора:**
- 🔐 **Эксклюзивный доступ**: Только номер `+7991816358` имеет доступ к админ-панели
- 📊 **Полная статистика**: Дашборд с активностью пользователей, сообщениями, SMS
- 👥 **Управление пользователями**: Просмотр, блокировка, мониторинг активности
- 💬 **Модерация чатов**: Просмотр и управление групповыми чатами
- 📱 **Контроль SMS**: Логи отправок, тестовые рассылки, шаблоны
- ⚙️ **Системные настройки**: Мониторинг здоровья приложения, логи

## 🏗️ **АРХИТЕКТУРА ДЛЯ RAILWAY.COM**

**Стек технологий:**
- **Frontend**: Vue 3 + Composition API + TypeScript + Vite + Pinia
- **Backend**: Node.js + Express + Socket.IO + JWT
- **База данных**: PostgreSQL (Railway Add-on)
- **Кэширование**: Redis (опционально, для кэширования SMS кодов)
- **Хранилище файлов**: Railway Volumes (`/data/uploads`)
- **СМС-сервис**: SMSC.RU API
- **Деплой**: Railway CLI + Nixpacks

## 🎨 **ДИЗАЙН И UI/UX (PIXEL-PERFECT)**

**Требования к интерфейсу:**
1. **Макет**: Точная трехколоночная структура Telegram Web
   - Левая колонка: Список чатов, поиск, меню
   - Центральная: Сообщения, ввод текста
   - Правая: Информация о чате/пользователе

2. **Темы**: Полная поддержка тем
   - Светлая: `#FFFFFF` фон, `#0000` текст
   - Темная: `#0E1621` фон, `#FFFFFF` текст
   - Мгновенное переключение без перезагрузки

## 🔐 **СИСТЕМА АУТЕНТИФИКАЦИИ ЧЕРЕЗ SMSC.RU**

**Процесс регистрации:**
```
1. Ввод номера → 2. Отправка SMS → 3. Ввод кода → 4. Создание профиля
```

## 📁 **ХРАНЕНИЕ ФАЙЛОВ НА RAILWAY VOLUMES**

**Архитектура хранилища:**
```
/data/uploads/
├── avatars/          # Аватарки пользователей
├── media/            # Фото/видео в чатах
│   ├── chat_uuid1/
│   └── chat_uuid2/
├── documents/        # Документы
└── temp/             # Временные загрузки
```

## 🗃️ **СХЕМА БАЗЫ ДАННЫХ (POSTGRESQL)**

```sql
-- Пользователи с флагом администратора
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone_number VARCHAR(20) UNIQUE NOT NULL,
  first_name VARCHAR(100),
  last_name VARCHAR(100),
  avatar_url TEXT,
  is_admin BOOLEAN DEFAULT false,
  is_banned BOOLEAN DEFAULT false,
  is_online BOOLEAN DEFAULT false,
  last_seen TIMESTAMP DEFAULT NOW(),
 created_at TIMESTAMP DEFAULT NOW()
);

-- Чаты
CREATE TABLE chats (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  type VARCHAR(10) CHECK (type IN ('private', 'group', 'channel')),
  title VARCHAR(255),
  avatar_url TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Сообщения с медиа
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  chat_id UUID REFERENCES chats(id) ON DELETE CASCADE,
  sender_id UUID REFERENCES users(id),
  content TEXT,
  file_type VARCHAR(20),
  file_url TEXT,
  file_name VARCHAR(500),
  file_size INTEGER,
  mime_type VARCHAR(100),
  is_edited BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Логи SMS для админ-панели
CREATE TABLE sms_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  phone_number VARCHAR(20) NOT NULL,
  message TEXT NOT NULL,
  sms_id VARCHAR(100),
  status VARCHAR(20),
  sent_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT NOW()
);

-- Действия администратора
CREATE TABLE admin_actions (
  id UUID PRIMARY DEFAULT gen_random_uuid(),
  admin_id UUID REFERENCES users(id) NOT NULL,
  action_type VARCHAR(50) NOT NULL,
  target_id UUID,
 ip_address INET,
  user_agent TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Автоназначение прав для +79918816358
INSERT INTO users (phone_number, is_admin) 
VALUES ('+7991816358', true)
ON CONFLICT (phone_number) 
DO UPDATE SET is_admin = true;
```

## 💬 **РЕАЛЬНОЕ ВРЕМЯ С SOCKET.IO**

**События WebSocket:**
```javascript
// Подключение
socket.on('connection', (user) => {
  socket.join(`user:${user.id}`);
  socket.join(`chat:${chatId}`);
  
  // Уведомление о входе администратора
  if (user.phone === '+79918816358') {
    io.emit('admin_online', user.id);
  }
});

// Основные события
socketEvents = {
  'message:new': (data) => io.to(`chat:${data.chatId}`).emit('message', data),
  'typing:start': (data) => socket.to(`chat:${data.chatId}`).emit('typing', data),
  'presence:online': (userId) => io.emit('user_online', userId),
  
  // Административные события
  'admin:user_banned': (data) => io.emit('user_banned', data),
  'admin:broadcast': (data) => io.emit('admin_message', data),
};
```

## 🐳 **КОНФИГУРАЦИЯ ДЛЯ RAILWAY**

**Файл `railway.toml`:**
```toml
[build]
builder = "NIXPACKS"

[environments]
  [environments.production]
    # Бэкенд
    PORT = "3000"
    JWT_SECRET = "{{#randomString 64}}{{/randomString}}"
    NODE_ENV = "production"
    
    # SMSC.RU
    SMSC_LOGIN = "{{must_env SMSC_LOGIN}}"
    SMSC_PASSWORD = "{{must_env SMSC_PASSWORD}}"
    SMSC_SENDER = "TelegramApp"
    
    # Хранилище
    UPLOADS_PATH = "/data/uploads"
    
    # Фронтенд
    VITE_API_URL = "https://api.ваш-домен.railway.app"
    VITE_WS_URL = "wss://api.ваш-домен.railway.app"

[services.backend]
name = "backend"
root = "./backend"
startCommand = "node dist/server.js"
healthCheckPath = "/health"

[[services.backend.volumes]]
name = "uploads-volume"
mountPath = "/data/uploads"

[services.backend.ports]
- port = 3000
  httpPath = "/"

[services.frontend]
name = "frontend" 
root = "./frontend"
startCommand = "npm run preview"

[services.frontend.ports]
- port = 4173
  httpPath = "/"
```

## ✅ **КРИТЕРИИ ЗАВЕРШЕНИЯ (MVP)**

**Основные функции:**
- [x] Регистрация через SMS (SMSC.RU) с лимитами
- [x] Pixel-perfect интерфейс Telegram Web
- [x] Обмен сообщениями в реальном времени
- [x] Отправка/прием изображений через Railway Volumes
- [x] Индикаторы онлайн/офлайн, "печатает..."
- [x] Переключение светлой/темной тем

**Админ-панель для +79918816358:**
- [x] Дашборд со статистикой
- [x] Управление пользователями (просмотр, блокировка)
- [x] Просмотр всех чатов и сообщений
- [x] Мониторинг отправки SMS
- [x] Системные логи и метрики

**Railway оптимизации:**
- [x] Health checks (`/health`)
- [x] Автоматический перезапуск
- [x] Логи в Railway Dashboard
- [x] Spending limit ($10-20 для начала)

## 🔧 **СТРУКТУРА ПРОЕКТА**

```
telegram-clone/
├── backend/
│   ├── src/
│   │   ├── controllers/
│   │   │   ├── auth.controller.js
│   │   │   ├── admin.controller.js    # Админ-панель
│   │   │   └── chat.controller.js
│   │   ├── services/
│   │   │   ├── sms.service.js         # SMSC.RU
│   │   │   ├── storage.service.js     # Railway Volumes
│   │   │   └── admin.service.js       # Логика админа
│   │   ├── middleware/
│   │   │   ├── auth.middleware.js
│   │   │   └── admin.middleware.js    # Проверка прав
│   │   ├── sockets/
│   │   │   └── chat.handlers.js
│   │   └── server.js
│   ├── Dockerfile
│   └── package.json
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── AdminPanel.vue         # Админ-панель
│   │   │   ├── ChatList.vue
│   │   │   └── MessageInput.vue
│   │   ├── views/
│   │   │   ├── AdminView.vue          # /admin
│   │   │   └── ChatView.vue
│   │   ├── stores/
│   │   │   ├── auth.store.ts
│   │   │   └── admin.store.ts         # Состояние админа
│   │   └── App.vue
│   ├── Dockerfile
│   └── vite.config.ts
└── railway.toml
```

## 🚨 **ВАЖНЫЕ ПРЕДУПРЕЖДЕНИЯ**

### **Юридические аспекты:**
1. **Название**: Не используйте "Telegram" в публичном домене
2. **Disclaimer**: Добавьте "Неофициальный клон для образовательных целей"
3. **SMSC.RU**: Получите утвержденное имя отправителя перед продакшеном

### **Безопасность:**
1. **Секреты**: Никогда не коммитьте `.env`, используйте `railway variables`
2. **Админ-доступ**: Только `+79918816358`, IP-фильтрация опционально
3. **JWT**: Короткие сессии для админа (8 часов), длинные для пользователей (7 дней)
4. **Spending limit**: Установите $10-20 в настройках Railway

### **Производительность:**
1. **Кэширование**: Redis для SMS кодов и сессий
2. **Индексы**: Добавьте индексы на часто запрашиваемые поля
3. **Мониторинг**: Используйте встроенные метрики Railway
4. **Бэкапы**: Настройте регулярное копирование PostgreSQL

## 🚀 **БЫСТРЫЙ СТАРТ**

### **Для разработки:**
```bash
# Клонирование и запуск
git clone [репозиторий]
cd telegram-clone

# Бэкенд (порт 3000)
cd backend
npm install
npm run dev

# Фронтенд (порт 5173)  
cd ../frontend
npm install
npm run dev

# Тестовый вход для админа:
# Номер: +79918816358
# Код: смотри в консоли бэкенда (разработка)
# Админ-панель: http://localhost:5173/admin
```

### **Для продакшена на Railway:**
```bash
# 1. Создайте аккаунт на https://railway.app
# 2. Подключите GitHub репозиторий к Railway
# 3. Railway автоматически определит сервисы и начнет деплой
# 4. Настройте переменные окружения:
#    - SMSC_LOGIN (ваш логин от SMSC.RU)
#    - SMSC_PASSWORD (ваш пароль от SMSC.RU)
# 5. Добавьте Volume для хранения файлов:
#    - Перейдите в Settings -> Volumes
#    - Создайте новый Volume с mount path "/data/uploads"
# 6. Настройте домены (опционально):
#    - Перейдите в Settings -> Domains
#    - Добавьте ваш кастомный домен или используйте railway.app
# 7. Установите spending limit:
#    - Перейдите в Settings -> Billing
#    - Установите лимит (рекомендуется $5-20 в месяц)
# 8. После деплоя:
#    - Frontend будет доступен по главному URL
#    - Backend будет доступен по URL/api
#    - Админ-панель: /admin (для номера +79918816358)
```

### **Первичная настройка администратора:**
```sql
-- Принудительное назначение прав админа
UPDATE users SET is_admin = true WHERE phone_number = '+79918816358';

-- Проверка
SELECT phone_number, is_admin FROM users WHERE is_admin = true;
```

## 📞 **ПОДДЕРЖКА И МОНИТОРИНГ**

### **Для администратора (+79918816358):**
1. **Дашборд**: `ваш-домен.railway.app/admin`
2. **Логи**: Railway Dashboard → Logs
3. **Метрики**: Railway Dashboard → Metrics
4. **База данных**: Railway Dashboard → PostgreSQL → Connect

### **Аварийные команды:**
```bash
# Перезапуск сервисов
railway restart --service backend
railway restart --service frontend

# Просмотр логов
railway logs --service backend -f

# Проверка здоровья
curl https://api.ваш-домен.railway.app/health

# Резервное копирование БД
railway psql -c "\copy (SELECT * FROM users) TO 'users_backup.csv' CSV HEADER"
```

---

**Готовый к деплою промпт содержит:** 
1. Полнофункциональный клон Telegram Web
2. Эксклюзивную админ-панель для `+7991816358`
3. Интеграцию с SMSC.RU для SMS
4. Хранение файлов на Railway Volumes
5. Автоматическую конфигурацию для Railway
6. Все необходимые меры безопасности и оптимизации